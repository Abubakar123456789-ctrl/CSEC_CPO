n=int(input())
for I in range (n):
    b=input ()
    m=list(map(int, input(). split()));x,y=[x for x in m if x%2==0],[x for x in m if x%2==1]
    if len(m)%2==1:
        print (-1)
    else:
        if len(x)>len(y):
            print ((len(x)-len(y))//2)
        elif len(y)>len(x):
            print ((len(y)-len(x))//2)   
        else :
            print (0)  
        